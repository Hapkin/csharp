@model IEnumerable<MVC_Tuincentrum2.DB.Soorten>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Soort)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MagazijnNr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Soort)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MagazijnNr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SoortNr }) |
            @Html.ActionLink("Details", "Details", new { id=item.SoortNr }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SoortNr })
        </td>
    </tr>
}
    
</table>
<a href="~/Soorten/ZoekForm" class="btn btn-primary">zoeken?</a>
